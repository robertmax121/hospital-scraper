[phases.install]
cmds = [
  "pip install aiohttp==3.9.3 supabase==2.4.0 python-dotenv==1.0.0 playwright==1.44.0 greenlet==3.0.3",
  "playwright install --with-deps chromium"
]

[start]
cmd = "python scheduler.py"
